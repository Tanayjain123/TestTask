<h1> create comments</h1>
<% if @task.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@comments.errors.count, "error") %> prohibited this article from being saved:</h2>
    <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_for [@project,@task,@task.comments.build] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div class="mb-3 mt-3">
    <%= f.label :addcomment %><br>
    <%= f.text_field :comment %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
